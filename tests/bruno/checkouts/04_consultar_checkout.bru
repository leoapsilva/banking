meta {
  name: 04 Consultar checkout criado
  type: http
  seq: 4
}

get {
  url: {{base_url}}/v1/checkouts/{{checkout_id}}?baas={{baas}}
  auth: none
}

assert {
  res.status: eq 200
  res.body.id: eq {{checkout_id}}
  res.body.status: isDefined
}
