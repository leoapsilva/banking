meta {
  name: 01 Registrar webhook de checkout no C6
  type: http
  seq: 1
}

post {
  url: {{base_url}}/v1/webhooks/register
  body: json
  auth: none
}

body:json {
  {
    "baas": "{{baas}}",
    "service": "CHECKOUT",
    "url": "{{webhook_public_url}}"
  }
}

assert {
  res.status: eq 200
}
